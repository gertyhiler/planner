{
  "name": "@planner/api-client",
  "version": "0.1.0",
  "description": "TypeScript API client for Planner",
  "main": "generated/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "generate": "bun scripts/generate-client.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@planner/api-schema": "workspace:*",
    "@planner/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "openapi-typescript-codegen": "^0.27.0"
  }
}
